#!/usr/bin/env python

from distutils.core import setup

setup(name='PRICE',
      version='0.0.1',
      description='Fiserv PRICE API Client',
      author='Fiserv',
      author_email='',
      packages=[],
      requires=['PrettyTable', 'requests'],
      )